#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    scanf("%d", &n);
    if(n<=5)
    {
        printf("%d" ,300*n);
    }
    else
    {
        printf("%d" ,280*n);
    }
    system("pause");
    return 0;
}